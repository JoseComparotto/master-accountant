import { AccountClassTheme } from './account-class-theme';

describe('AccountClassTheme', () => {
  it('should create an instance', () => {
    const directive = new AccountClassTheme();
    expect(directive).toBeTruthy();
  });
});
