import { AccountTheme } from './account-theme';

describe('AccountTheme', () => {
  it('should create an instance', () => {
    const directive = new AccountTheme();
    expect(directive).toBeTruthy();
  });
});
