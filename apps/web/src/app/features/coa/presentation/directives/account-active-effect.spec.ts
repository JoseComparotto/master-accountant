import { AccountActiveEffect } from './account-active-effect';

describe('AccountActiveEffect', () => {
  it('should create an instance', () => {
    const directive = new AccountActiveEffect();
    expect(directive).toBeTruthy();
  });
});
