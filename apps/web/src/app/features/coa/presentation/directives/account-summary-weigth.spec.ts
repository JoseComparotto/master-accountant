import { AccountSummaryWeigth } from './account-summary-weigth';

describe('AccountSummaryWeigth', () => {
  it('should create an instance', () => {
    const directive = new AccountSummaryWeigth();
    expect(directive).toBeTruthy();
  });
});
